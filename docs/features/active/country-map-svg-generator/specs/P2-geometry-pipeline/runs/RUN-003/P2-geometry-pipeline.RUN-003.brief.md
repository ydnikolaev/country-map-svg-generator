---
# SCAFFOLDED by mate from a versioned governed template; AUTHORABLE INSTANCE.
schema_version: "1.2.0"
template_version: "1.2.0"
kind: "run-brief"
id: "RUN-003"
epic: "country-map-svg-generator"
spec: "P2"
run: "RUN-003"
status: running
profiles: []
concerns: []
inputs: ["PLAN-003", "CTX-RUN-005"]
---
# RUN-003 — Integrated LOD geometry delivery

## Authority and accepted inputs

- Accepted PLAN-003 revision:
  `50a1404037c20a8a2fdb7ea9df3d685a756fe06d8b4a040fe52028c3398d278e`.
- Accepted PLAN-003 manifest:
  `3ea7b881765c64fd5543b32071e623edb074925a32544ec1f77e61d6c9f51f79`.
- Runtime context CTX-RUN-005:
  `dd4f83d709b541f9f162eb2312bc73153421f8975b7ba80551d3a9e799c15ab7`.
- Product baseline commit `c2ea11c56c0af83aa25a0cd58188864cb0129275`,
  tree `82e476d45052078f87ccc4d4bda06ab0a689cdd7`; lifecycle head at run
  authoring is `aab51f8322fc1a6b3745f661c02e8bf29b074aed`.
- DEC-003, DEC-004, AM-001, AM-002, P1/CTR-001 and interrupted
  RUN-001/RUN-002 are binding through PLAN-003. Readiness binds atomically at
  run start.

## Objective and completion boundary

Deliver CTR-002 with natural-aspect `tight`, arbitrary distortion-free `contain`,
scale-only compact/standard/source LOD, full-source retention/restoration,
robust topology/deviation guards, deterministic minified fixed-grid SVG path
commands, generic hard budgets, optional bounded softening and shared markers.

T0 is the integrated pre-publication brake and owns every production behavior it
tests. It temporarily injects all-283 candidate LOD tables into the exact
production path, covers all 249 entities, both profiles, required scales and
frames, and requires card `<=2,500`, hero `<=8,000`, structural/protection/
deviation/determinism success and owner-approved representative visuals. T1 may
not publish unless T0 passes. The run excludes P3 SVG styling/CLI parsing, P4
browser integration, public packaging and deferred `detail`.

## Scope ownership and footprint

Writable product paths are the exact PLAN-003 task union: `Makefile`, `go.mod`,
`go.sum` and the declared `internal/geometry/**` files. Sequential exclusive
leases are:

- T0: maintainer builder/tool/recipe/spike evidence plus production projection,
  layout, LOD injection/selection, retention/restoration, topology/deviation,
  canonical serializer, budgets, softening, presets, overrides and markers;
- T1: compact/standard/manifest artifacts, generated embedding adapter and
  offline rebuild validator;
- T2: approval/integration/oracle/mutation evidence and `Makefile`.

`data/**` and `internal/catalog/**` are read-only authorities. `cmd/**`,
`internal/config/**`, `internal/render/**`, `.git/**`, `.mate/**`, lifecycle
docs and another task's lease are forbidden to implementers. Integration target
is `main`; implementers return scoped changes without lifecycle mutations.

## Context and doctrine pack

CTX-RUN-005 supplies the current registered implementation context. PLAN-003
supplies the exact successor HOW. Mapshaper 0.7.44, Node and npm are allowed only
inside the pinned maintainer rebuild workflow. Ordinary Go generation and
`make check` remain offline and do not execute or require them.

RUN-001/RUN-002 Go files are untrusted scratch. Reuse is allowed only inside the
active task lease after reconciliation with PLAN-003. Chat history is not
authority.

## Tasks

1. T0: implement and pass the integrated production-path LOD brake; search only
   the accepted global calibration ranges; freeze the recipe; generate and obtain
   approval for the representative visual sheet.
2. T1: publish and embed the exact T0-frozen all-283 compact/standard artifacts
   and manifest with deterministic provenance.
3. T2: allocate full-corpus, D3, layout, selection, restoration, serializer,
   budget, visual, override and marker evidence and pass offline `make check`.

Only T0 is dispatchable initially. Later tasks require the prior task's verified
receipt and scoped product commit.

## Validation and evidence

- T0: `GOWORK=off go test ./internal/geometry/... -run
  'TestLODIntegratedBrake' -count=1`, plus focused serializer/topology tests.
- T0 evidence names all 283 IDs; every entity/profile; every requested scale and
  frame; final path-byte maxima; selected tiers; fallbacks; restored/removed
  source parts; protected anchors; artifact bytes/runtime/hashes; two
  byte-identical rebuilds; and the visual sheet.
- T1: pinned double rebuild equals committed bytes; offline artifact validator
  proves source/tool/recipe/hash/coverage/omission bindings.
- T2: D3 parity, topology/serializer teeth, overrides, markers, full package
  tests and offline `make check`.

Representative green cases never substitute for the all-corpus brake. Human
visual approval is mandatory evidence, not inferred from structural tests.

## Constraints and forbidden actions

- Select LOD only from effective fitted geometry scale; never ISO, profile,
  preset name or frame shape.
- Compute projector, natural aspect, fit, retention and markers from full P1
  geometry before reading a LOD.
- Restore required full-source components/rings generically before validation.
- A shared endpoint is allowed only when tied to the same full-source vertex;
  proper crossings, new touches, winding changes, escaped holes and collapse fail.
- Bound LOD and softened-curve displacement in fitted space by the resolved
  tolerance; never silently raise tolerance or precision.
- Canonical path minification must round-trip to exact commands through the
  strict parser; no SVGO or post-hoc optimizer.
- `MaxPathBytes` is generic data. Fallback code cannot inspect preset names.
- Never country-branch, hand-redraw, bridge or synthesize geography.
- Never add runtime Node/Python/GDAL/PROJ/CGO/network or weaken a topology,
  protection, budget, deterministic rebuild, visual or project gate.

## Stop escalation and resume conditions

Stop before mutation for a new tier, dependency/tool version, algorithm,
calibration outside accepted ranges, hard budget, selection input, serializer
grammar family, source root or runtime external process. Stop for changes to
natural/contain semantics, P1 authority, protected policy, presentation-free
CTR-002 or the offline single-binary contract.

T0 stops without product commit if any hard maximum, topology, deviation,
protection, provenance, visual approval, artifact-size/runtime or deterministic
rebuild brake fails. After repeated speculative fixes, isolate a minimal fixture
and consult the owning canonical documentation. Resume only from PLAN-003, this
brief, CTX-RUN-005, the exact lease, current scoped diff and last verified result.

<!-- MATE:extensions — generated by composition from selected profiles and concerns -->

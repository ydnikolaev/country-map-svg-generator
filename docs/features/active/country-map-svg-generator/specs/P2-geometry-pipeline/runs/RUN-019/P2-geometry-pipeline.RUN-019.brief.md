---
# SCAFFOLDED by mate from a versioned governed template; AUTHORABLE INSTANCE.
schema_version: "1.2.0"
template_version: "1.2.0"
kind: "run-brief"
id: "RUN-019"
epic: "country-map-svg-generator"
spec: "P2"
run: "RUN-019"
status: running
profiles: []
concerns: []
inputs: ["PLAN-012", "CTX-RUN-021"]
---
# RUN-019 — Variable-resolution component brake and representative checkpoint

## Authority and accepted inputs

Execute accepted PLAN-012
`d2503dbc1039d6984ed32e9427b6b1f25210d42c037cc4abdcb279065ce6b6ea`
from baseline commit `8bfeeae9f46fd5a93daf7ee2aaf86356cc468891`, tree
`33a25c074df75e9382c8de2099ebbb054f8894f1`.

Bind sealed candidate plan/manifest
`d8b94e354b96079ba3b8dab443b595b15377c6867629dab19584539992d6752e` /
`f7559ef287fecec2ed645118047e135af243ed352ba9947f13e3048248c39c0d`,
RUN-018 result
`0dcf517debb6bb0e04c853b9734d3ea38622568ba9cdcdcbe8ca3937abe9f1b0`,
oracle
`f2c9cd32e806985be55193e564942e716bb42c72e01d7405c976ddf36666d56f`,
predecessor recipe
`da3ff9d331df37882bb2ed4159e9f14ef2aaeedf2b9fcff9e77c79fed4d751c5`
and CTX-RUN-021
`eabe804f68b2073aad9e946dec5df8e9edf5c403ba010c9d5175051a2ce87d67`.

## Objective and completion boundary

Execute PLAN-012 T0A only. Implement pinned Mapshaper variable component
resolution and `lineage-features/v2`; pass the synthetic semantics brake, SH
brake, CA/ID/KI/Indonesia brake and unchanged-policy representative machine
gate; rebuild representative artifacts twice and stop with the new contact
sheet for owner review.

Do not enter T0B, generate the full 996-output catalog, wire runtime publication,
stage or commit product bytes. T0A is complete only when every machine gate
passes and the exact new sheet/receipt hashes are available for owner approval.

## Scope ownership and footprint

Exclusive writes:

- `internal/geometry/cmd/lodbuild/**`;
- `internal/geometry/lod/tool/build.mjs`;
- `internal/geometry/lod/v2.recipe.json`;
- `internal/geometry/silhouette.go`;
- `internal/geometry/silhouette_test.go`;
- `internal/geometry/testdata/silhouette-oracle/**`.

Preserve existing product scratch outside the lease. Do not edit `go.mod`,
`go.sum`, `data/**`, `internal/catalog/**`, runtime `lod.go`/`lod_test.go`,
oracle JSON, v1 recipe, package manifests, serializer, presets, overrides,
public `cmd/**`, lifecycle documents or another task lease. Never stage
`node_modules` or temporary artifacts.

## Context and doctrine pack

Load CTX-RUN-021 before implementation. It carries current P1 source-corpus
authority. PLAN-012 and this brief carry the exact geometry policy and runtime
fences. The user-visible intent remains lightweight, soft-organic,
aspect-ratio-correct country SVGs for arbitrary viewports; `detail` remains
backlog.

## Tasks

1. Add a synthetic two-component control and prove pinned Mapshaper `0.7.44`
   variable resolution empirically. `base*country_long/component_long` must
   equal the single-component constant-control interval and differ from
   country-relative constant resolution. Add typed teeth for constant-mode
   substitution, inverted ratio, zero/non-finite dimensions and argv/expression
   drift.
2. Implement `lineage-features/v2`: exact keys, deterministic numeric resolution
   properties, all components of one geometry in one FeatureCollection, exact
   output-key validation, hole matching and source-order reassembly.
3. Build the all-consumer mandatory union for each geometry/band. Mandatory is
   visible, dominant, anchored or group-anchored for any resolved consumer.
   Optional identities/subscale components may be omitted only with truthful
   frozen provenance. No entity/profile/preset/frame candidate branching.
4. Run SH over all frozen resolutions for both consumers and bands. Require all
   four lineage outputs before pruning, component 0 recall at least `.50`,
   nonzero contribution, topology and aggregate budgets. Stop at the first
   unsatisfied frozen invariant.
5. If SH passes, run CA, ID, KI and the Indonesia `120/121` legacy control plus
   new candidates. Require consumer-order invariance, mandatory fidelity,
   truthful optional omission and `2200/7500`, `<2500/<8000` budgets.
6. If all brakes pass, run lineage/hole/provenance/budget/forbidden-change
   mutations, full existing builder regressions and two byte-identical
   representative builds. Emit the new recipe, manifest, machine receipt and
   contact sheet, then stop for owner approval.

## Validation and evidence

Required evidence:

- exact pinned Mapshaper version/help and variable-semantics control;
- focused synthetic and lineage mutation tests;
- SH semantic evidence twice with identical digest;
- CA/ID/KI and Indonesia per-band maxima, metrics, omissions and timings;
- frozen oracle/v1 recipe/serializer hashes;
- semantic recipe diff limited to PLAN-012 candidate-source HOW;
- full `go test ./internal/geometry/cmd/lodbuild -count=1`;
- focused geometry silhouette/component/topology/protection tests;
- two representative builds with byte-identical manifest, receipt and sheet;
- exact changed-path inventory excluding `node_modules`.

## Constraints and forbidden actions

Keep centered LAEA, q `.01`, the 29-resolution order, weighted Visvalingam
`.7`, planar/keep-shapes/clean/intersection repair, visibility thresholds,
IoU/recall/component recall, byte caps, serializer and pure-Go offline runtime.

No country literal/branch, manual vertex, custom simplifier, MakeValid,
union/buffer repair, threshold/budget calibration, source-corpus change,
projection change, CGO or runtime Node/Mapshaper dependency. Owner approval
cannot waive a machine failure.

## Stop escalation and resume conditions

Stop immediately and restore the exact pre-run scratch inventory if:

- pinned variable behavior contradicts the derived interval;
- SH has no passing frozen candidate;
- worst-multipart fidelity necessarily exceeds a hard budget;
- a country-specific rule, manual geometry or frozen-contract change is needed;
- outputs drift between identical runs; or
- representative machine validation fails.

Return exact geometry/band/resolution/lineage/metrics/bytes evidence and the
narrowest next decision. On success, return exact changed paths, tests, timings,
recipe/manifest/receipt/sheet hashes and sheet path. Resume only after the
coordinator records machine evidence and the owner explicitly approves the new
sheet.

<!-- MATE:extensions — generated by composition from selected profiles and concerns -->

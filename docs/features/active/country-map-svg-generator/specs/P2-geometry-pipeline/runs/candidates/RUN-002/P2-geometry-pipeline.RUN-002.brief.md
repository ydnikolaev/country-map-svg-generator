---
# SCAFFOLDED by mate from a versioned governed template; AUTHORABLE INSTANCE.
schema_version: "1.2.0"
template_version: "1.2.0"
kind: "run-brief"
id: "RUN-002"
epic: "country-map-svg-generator"
spec: "P2"
run: "RUN-002"
status: draft
profiles: []
concerns: []
inputs: ["PLAN-002", "CTX-RUN-004"]
---
# RUN-002 — Run brief

## Authority and accepted inputs

- Accepted successor plan revision PLAN-002:
  `eca52910a9719aa71825917bf17e7ea74ecdffc4325bb483a2ffc379bd94b8b2`.
- Accepted PLAN-002 manifest:
  `1f39e250c9edab3faf0be60bca044b9b3a0043ac6f7c63eab5765268c270eb7b`.
- Runtime context CTX-RUN-004:
  `11e319291722631f226ab3912ea29e5e0a3e82f8eb0a25655606296981682228`.
- Baseline commit `50f56191c21dc84530f579c4896a59d3a3651476`, tree
  `5d812dc559e47c8a33b67e99fa61d0888349473c`; lifecycle descendant at
  run authoring is `7edddc21ac3eb18cfa0a8c2295c886ffb78d3725`.
- DEC-003, DEC-004, AM-001, AM-002, P1/CTR-001 and RUN-001-RESULT are
  binding through the accepted plan. Run readiness is bound atomically at start.

## Objective and completion boundary

Deliver CTR-002 with natural-aspect `tight`, arbitrary distortion-free `contain`,
scale-driven compact/standard/source LOD, lossless full-source fallback, exact
retention/restoration provenance, fixed-grid commands, optional bounded
softening and shared markers.

T0 is a mandatory brake. It must temporarily cover all 283 geometries, 249
entities, both profiles and both presets, pass topology/protection/determinism and
2,500/8,000-byte hard maxima, and obtain representative visual approval. T1–T4
may not begin without its passing receipt. If T0 fails, finish RUN-002 blocked
without a production artifact commit. This run excludes P3 SVG/XML styling and
CLI parsing, P4 browser integration, public packaging and deferred `detail`.

## Scope ownership and footprint

Writable product paths are the PLAN-002 union: `Makefile`, `go.mod`, `go.sum`,
and `internal/geometry/**`. Sequential task leases are exclusive:

- T0: builder, maintainer tool, v1 recipe and spike evidence;
- T1: compact/standard/manifest artifacts and the rebuild test;
- T2: runtime contract, projection/layout integration, LOD selection/guards,
  presets/overrides and markers;
- T3: retention, paths and softening;
- T4: project gate and approval/oracle/mutation evidence.

`data/**` and `internal/catalog/**` are read-only authority. `cmd/**`,
`internal/config/**`, `internal/render/**`, `.git/**`, `.mate/**`, lifecycle
docs and another task's lease are forbidden to implementers. The integration
target is `main`; implementers return scoped product commits only.

## Context and doctrine pack

CTX-RUN-004 supplies the registered current doctrine. PLAN-002 and DEC-004 add
the exact LOD runtime boundary: Mapshaper 0.7.44, Node and npm are allowed only
inside the explicit maintainer rebuild workflow with exact package lock and
integrity. Ordinary Go generation and `make check` remain offline and do not
execute or require them.

The RUN-001 draft in the worktree is untrusted scratch. A worker may reuse only
bytes inside its active lease after reconciling them with PLAN-002 and removing
diagnostic/adaptive-precision/custom-repair probes. Chat history is not authority.

## Tasks

1. T0: finish the production-capable LOD builder; calibrate the bounded
   compact/standard recipes and generic scale thresholds; run the all-corpus
   structural/budget/determinism brake and representative visual sheet.
2. T1: publish deterministic corpus-bound compact/standard artifacts and
   manifest from the frozen T0 builder/tool/recipe.
3. T2: integrate full-source-first retention, scale-only tier selection,
   component/ring matched symmetric deviation, topology guards, generic
   restoration, additive provenance and source fallback without layout drift.
4. T3: complete removal/restoration accounting, fixed `0.01px` commands,
   `MaxPathBytes`, and topology-checked optional softening with linear fallback.
5. T4: allocate D3, full-corpus, natural/arbitrary layout, override, marker,
   mutation, visual/budget and offline project evidence.

## Validation and evidence

- T0 receipt: all 283 geometry IDs; all entities/profiles/presets; card
  `<=2,500`, hero `<=8,000`; representative human visual sheet; exact artifact
  size/runtime; two byte-identical clean builds.
- `geometry-lod-rebuild-check`: pinned temporary double rebuild equals committed
  artifacts.
- `geometry-lod-runtime`, `geometry-topology-teeth`, `geometry-d3-parity`,
  `geometry-overrides`, and `geometry-markers` use the exact PLAN-002 commands.
- Final ceiling: `GOWORK=off go test ./internal/geometry -count=1`,
  `GOWORK=off go test ./... -count=1`, and offline `make check`.

Evidence must name exact commands, output hashes, path bytes, selected tiers,
fallbacks, restored/removed source parts, protected anchors, final product
commit/tree and any degradation. Representative green output never substitutes
for the all-corpus brake.

## Constraints and forbidden actions

- Select LOD only from effective fitted geometry scale; never ISO, profile,
  preset name or frame shape.
- Compute layout and retention decisions from full P1 geometry; never let LOD
  change natural aspect, fit, marker transform or required components/rings.
- Require component/ring matched symmetric deviation within the resolved
  tolerance. Never silently raise tolerance or precision.
- `MaxPathBytes` is generic data: preset defaults resolve it, but fallback logic
  cannot inspect preset names.
- Never hand-redraw, bridge, synthesize or country-branch geography.
- Never mutate P1, accepted LOD bytes or recipe in place; version changes.
- Never add runtime Node/Python/GDAL/GEOS/PROJ/CGO/network, SVGO, or another
  post-hoc optimizer.
- Never weaken a topology, protection, budget, deterministic rebuild, visual or
  project-ceiling gate to obtain green.

## Stop escalation and resume conditions

Stop before mutation for a new tier, dependency/tool version, algorithm,
calibration outside accepted ranges, budget maximum, selection input, source
root, public provenance family or runtime external process. Stop for any change
to natural/contain semantics, P1 authority, protected policy, presentation-free
CTR-002 or the offline single-binary contract.

T0 terminates the run blocked if a hard maximum, topology/protection/provenance,
visual approval, artifact-size/runtime or deterministic rebuild condition fails.
After several speculative fixes to one fault, reduce to a minimal fixture and
read the owning official documentation. Resume only from PLAN-002, this brief,
CTX-RUN-004, the exact active lease, current scoped diff and last verification
receipt.

<!-- MATE:extensions — generated by composition from selected profiles and concerns -->

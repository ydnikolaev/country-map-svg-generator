---
# SCAFFOLDED by mate from a versioned governed template; AUTHORABLE INSTANCE.
schema_version: "1.2.0"
template_version: "1.2.0"
kind: "run-brief"
id: "RUN-007"
epic: "country-map-svg-generator"
spec: "P2"
run: "RUN-007"
status: draft
profiles: []
concerns: []
inputs: ["PLAN-007", "CTX-RUN-009"]
---
# RUN-007 — Bounded grid-phase micro-brake

## Authority and accepted inputs

- Accepted PLAN-007 revision:
  `813d3a6551b9c6b008900ccb344f72dcc18d98bcbc9a72461e1d3dbfbc2adfdd`.
- Accepted PLAN-007 manifest:
  `bdc2f69fb6cc5346e94c45533685d44e11f97b7d156b909da51be1854790338e`.
- Runtime context CTX-RUN-009:
  `666ed2fbf2847ea2516e291cb73048229eab755674bec4e65f5a8f5a65a0f8f0`.
- PLAN-007 baseline is commit
  `0306704d328b487a4e558553d00646b2ab7d112b`, tree
  `f412b9f5fb8356d990d313d342cafcd8d0175575`; lifecycle head at run
  authoring is `d709a6b`.
- RUN-006-RESULT, RESULT-013, its plan invalidation, DEC-003/004/005,
  AM-001/002 and exact P1 authority bind through PLAN-007. Readiness binds
  atomically at run start.

## Objective and completion boundary

Execute PLAN-007 T0a only. Reconcile the existing PLAN-006 scratch, compose one
deterministic global q-grid phase exactly once through the candidate transform,
and prove forced-source RU card plus AQ card/hero under unlimited and normal
budgets. Add the nil-safe public `Generate` table seam, but do not publish T1
artifacts.

This dispatch stops at the T0a checkpoint even if it passes. T0b all-corpus
matrix, owner visual approval, the T0 foundation commit, T1 publication and T2
closure evidence require separate predecessor receipts.

## Scope ownership and footprint

The initial T0a implementer may edit only:

- `internal/geometry/gridphase.go`,
  `internal/geometry/gridphase_test.go`;
- `internal/geometry/lod.go`, `internal/geometry/lod_test.go`;
- `internal/geometry/model.go`, `internal/geometry/model_test.go`;
- `internal/geometry/pipeline.go`, `internal/geometry/pipeline_test.go`;
- `internal/geometry/cmd/lodbuild/main_test.go`;
- `internal/geometry/testdata/lod-spike/**`.

PLAN-007 T0 integration owns the complete uncommitted geometry foundation,
`go.mod` and `go.sum`, but those prerequisite bytes are read-only for this
implementer. The serializer is integration-owned and edit-frozen at
`serialize.go`
`c9ee9019b9155522acef16cd405ef0a72c93a4d519493a86061617cc2e034345`
and `serialize_test.go`
`0142dea0db810bfa97e4bb23d30cdf039faad860e8e1354a4111b804ee584c8f`.

All T1/T2 files, other `internal/geometry` files, `go.mod`, `go.sum`, `Makefile`,
`data/**`, `internal/catalog/**`, `cmd/**`, `docs/**`, `.git/**` and `.mate/**`
are forbidden to the implementer. Integration target is `main`; the implementer
returns scoped changes without commit or lifecycle mutation.

## Context and doctrine pack

CTX-RUN-009 supplies the current run doctrine; PLAN-007 supplies the exact HOW.
The existing uncommitted scratch is not accepted product and must be reused only
inside the accepted lease with fresh proof. Mapshaper 0.7.44 and Node remain
maintainer-only; runtime generation and ordinary checks stay pure Go and offline.

## Tasks

1. Add the exact 100-phase lexicographic schedule
   `(dx,dy) ∈ {0.000,…,0.009}²`, `(0,0)` first.
2. For each already reduced/restored tier candidate, compose a phase exactly
   once into its transform before final q canonicalization. Use the same
   resulting transform for the candidate, full reference, protected checks,
   shifted viewBox and later markers. Failed candidates must not leak phase
   state.
3. Run the unchanged final topology, structure, containment, protection,
   symmetric-deviation, serializer round-trip and byte predicates for every
   phase. Record attempted/selected phase in provenance.
4. Make public `Generate` route through a nil-safe published-table seam:
   nil means source-only; injected `GenerateWithLOD` behavior remains isolated.
5. Run only the RU/AQ T0a experiment and performance brake. Stop and return its
   exact result.

## Validation and evidence

For forced-source RU/un/card and AQ/un/card+hero, run equivalent unpresetted
`MaxPathBytes=0` first, then normal `2500/8000` budgets. Also rerun AQ
compact/standard through the centered-LAEA table.

For every case record tier, attempted/selected tolerance and phase, phase count,
per-phase and total runtime, q-grid proof, viewBox/transform/marker shift,
component/ring identity, protection/minimum-parts, raw/final deviation, points,
path bytes, parser round-trip and deterministic repeat. Assert:

- every path coordinate is on `0.01*Z`;
- phase is applied exactly once and viewBox width/height/aspect/centering are
  unchanged;
- visual rounding displacement stays within `q/sqrt(2)`;
- each RU/AQ generation is at most `10s` and the complete post-build micro-brake
  is at most `30s` on recorded YMBPM3;
- public `Generate` nil/source behavior and direct table injection both pass.

Focused Go tests and package compile are due. The normal-production `120s` warm
batch, full corpus, clean-checkout T0 receipt and project ceiling are T0b/T0
integration evidence, not this dispatch.

## Constraints and forbidden actions

Keep centered LAEA, zero build rotation, q=`0.01`, ratio `≤0.012`, floor
`0.80px`, explicit cap `1.25px`, LOD `64/256`, thresholds `240/700`, budgets
`2500/8000` and advisory `2200/7500`.

Phase schedule and selection are generic and may not inspect entity, profile,
preset, frame or tier. No MakeValid, union/buffer, noder, vertex-specific
movement, country branch, dependency, fork/replace, vendored internal code,
CGO, smaller q, larger tolerance, changed serializer, silent budget overflow,
new tier or runtime process is permitted.

## Stop escalation and resume conditions

Stop without product commit if all phases exhaust, any final guard fails, path
coordinates leave the grid, relative geometry/viewBox/marker placement changes,
serializer/dependency changes appear, or `10s/30s` is exceeded. Do not widen or
adapt the phase lattice.

If T0a passes, return control with exact evidence; T0b remains undispatched. If
it fails, preserve the exact tolerance×phase matrix as the decision-review
resume anchor. Changes to natural/contain semantics, P1 authority,
presentation-free CTR-002 or offline single-binary runtime require governed
amendment/decision.

<!-- MATE:extensions — generated by composition from selected profiles and concerns -->

---
# SCAFFOLDED by mate from a versioned governed template; AUTHORABLE INSTANCE.
schema_version: "1.2.0"
template_version: "1.2.0"
kind: "run-brief"
id: "RUN-017"
epic: "country-map-svg-generator"
spec: "P2"
run: "RUN-017"
status: draft
profiles: []
concerns: []
inputs: ["PLAN-010", "CTX-RUN-019"]
---
# RUN-017 — Saint Helena candidate-lineage diagnostic

## Authority and accepted inputs

- Accepted PLAN-010 plan/manifest:
  `a81da84fc3881068ad0520a3746f3276c18df87a48f10e83d5463fb03fb1db63` /
  `fdd0cdfa0c545410f102eb460fef80b57d3b03484a2fc0b1cb423975a7fff59d`.
- CTX-RUN-019:
  `433f2388f79881de55ee7a1f6d3932ca5d44778009eb7ff3481b4608aab83d8b`.
- RUN-016 result / RESULT-024:
  `e8396ed78ea7f2a7d61d0ad2ecbd8cb80a500a552c6299bd6423be436bbca33f` /
  `fc953a46c972153266bdb3fcdef7060e98f54558d2cce299145d937e8efe2943`.
- Frozen oracle/recipe:
  `f2c9cd32e806985be55193e564942e716bb42c72e01d7405c976ddf36666d56f` /
  `da3ff9d331df37882bb2ed4159e9f14ef2aaeedf2b9fcff9e77c79fed4d751c5`.

## Objective and completion boundary

Reproduce only the RUN-016 Saint Helena failure and determine its layer before
any plan or recipe change. Prove whether source component 0 is removed by
Mapshaper, preserved but displaced/reordered by projection or output parsing,
or rejected by a faulty oracle comparison. Produce deterministic per-resolution
evidence for `512..8`, then stop. Do not integrate runtime or scan the catalog.

## Scope ownership and footprint

Writable only:

- new `internal/geometry/cmd/lodbuild/sh_diagnostic_test.go`.

Everything else is read-only, including `.git`, `.mate`, `docs`, Go module
files, catalog/source data, runtime, builder implementation, oracle, recipes,
tool, representative artifacts and T0B/T1/T2 files. The diagnostic file may
remain only when it is a deterministic regression-quality test; otherwise
remove it before return. Never stage or commit.

## Context and doctrine pack

CTX-RUN-019 supplies P1 authority. PLAN-010 and RUN-016 are the exact HOW and
failure evidence. The geometry is
`geo-0a336e9d4ea30c295a4611bf7f13a0c6bbd8cad41c0440ad503c1c68c63e4ec7`,
used by SH/un and SH/de_facto. P1 has four one-ring polygons with 16, 18, 28
and 21 points; SH declares no protected features. Component 0 is reported as
contribution 1 but visible because it is dominant, so omission is forbidden.

## Tasks

1. Use the exact canonical projected-source and pinned `build.mjs` command for
   each frozen resolution. No alternative recipe or tool flags.
2. For source and output, record geometry type, polygon/ring/point counts,
   deterministic component digests, projected areas/bounds and nearest
   source-to-candidate overlap/recall matrix. Do not assume output order.
3. Evaluate the exact SH/un compact oracle path and record visibility flags,
   candidate contribution, recall, dominant/identity reason and typed failure.
4. Add mutation teeth that distinguish: a truly absent component, a reordered
   component, output-parser loss, projection/center mismatch and raster
   comparison error. A reorder must not count as loss.
5. Run twice and require byte-identical semantic evidence. Return the earliest
   layer where component 0 ceases to match and the narrowest authorized
   successor: builder bug, oracle bug, or recipe/candidate-source amendment.

## Validation and evidence

- `GOCACHE=/private/tmp/country-map-go-cache GOWORK=off go test
  ./internal/geometry/cmd/lodbuild -run '^TestSHCandidateLineageDiagnostic$'
  -count=1 -v`, twice;
- compile-only `./internal/geometry` and `./internal/geometry/cmd/lodbuild`;
- exact pre/post hashes for every existing file in the lease neighborhood.

The result must be diagnostic evidence, not a policy workaround.

## Constraints and forbidden actions

Do not alter thresholds, budgets, q, recipe resolutions/weighting, Mapshaper
flags, source geometry, component order, visibility rules or protected
provenance. No runtime integration, all-catalog scan, publication, owner gate,
country-specific production branch or false subscale classification.

## Stop escalation and resume conditions

Stop if exact reproduction needs a file outside the lease, the failure cannot
be reproduced from frozen inputs, or evidence remains ambiguous after one
bounded diagnostic. On PASS, return exact evidence and zero production changes
for coordinator review and a governed plan successor if required.

<!-- MATE:extensions — generated by composition from selected profiles and concerns -->

---
# SCAFFOLDED by mate from a versioned governed template; AUTHORABLE INSTANCE.
schema_version: "1.2.0"
template_version: "1.2.0"
kind: "run-brief"
id: "RUN-014"
epic: "country-map-svg-generator"
spec: "P2"
run: "RUN-014"
status: draft
profiles: []
concerns: []
inputs: ["PLAN-010", "CTX-RUN-016"]
---
# RUN-014 — Full-catalog geometry foundation and owner gate

## Authority and accepted inputs

- Accepted PLAN-010:
  `a81da84fc3881068ad0520a3746f3276c18df87a48f10e83d5463fb03fb1db63`;
  manifest
  `fdd0cdfa0c545410f102eb460fef80b57d3b03484a2fc0b1cb423975a7fff59d`.
- Runtime context CTX-RUN-016:
  `db5fc79c00a3f3a086a07edabb7e322fdf40bd4ad755a75f27e3829f81c52157`.
- RUN-013 checkpoint result:
  `e919b1e075d79b62ee09fe78fc7baa690357391b1de2d1be9dea05d985426cbf`;
  passing T0A gate
  `792fd75998656c10ecbd7bfdea760685c8f5880d613574b50210aa421339a0ba`;
  coder result
  `6c34549769804c3a0751954d3db9ee3aa271d0b20511ca34c9456d61389082ee`.
- Accepted owner decision DEC-008:
  `d92caff0294b08a46cb7d49dfda8e2309918afe0665a6a01d6a82fa55894d3de`.
- Frozen representative identities: oracle
  `f2c9cd32e806985be55193e564942e716bb42c72e01d7405c976ddf36666d56f`,
  recipe
  `da3ff9d331df37882bb2ed4159e9f14ef2aaeedf2b9fcff9e77c79fed4d751c5`,
  manifest
  `b2c467b08fb32603a312918da05ce83da355499fb8cba7f9e73c5a8e916995da`,
  sheet
  `cf1e568c93646047791b45a329e50a1308683e8c3aae03ee0944654db3718f9f`
  and machine receipt
  `fcc1bc307509d55aa281591f1cce0d0e78f73f323a41fc6d78e665a56c2dfd10`.

## Objective and completion boundary

Execute PLAN-010 T0B only. Integrate the remaining geometry foundation around
the frozen T0A oracle and selection policy, make both inherited production
spikes green without editing their assertions, and generate the deterministic
249-entity × 2-profile × 2-preset = 996-output machine corpus.

Prove budgets, natural and arbitrary contain layout, provenance, determinism,
offline runtime and the YMBPM3 `<=120s` warm-production ceiling. Render the
full-catalog review sheet from the exact accepted outputs and stop for product
owner approval. Do not publish the embedded all-283 ladder, wire T1 generated
tables, execute T2, stage or commit product bytes.

## Scope ownership and footprint

The implementer owns only PLAN-010 T0B paths:

- `go.mod`, `go.sum`;
- `internal/geometry/adapter.go`, `adapter_test.go`;
- `internal/geometry/fit.go`, `fit_test.go`;
- `internal/geometry/lod/tool/package.json`,
  `internal/geometry/lod/tool/package-lock.json`;
- `internal/geometry/lod_catalog_test.go`;
- `internal/geometry/marker.go`, `marker_test.go`;
- `internal/geometry/math.go`;
- `internal/geometry/normalize.go`, `normalize_test.go`;
- `internal/geometry/path.go`, `path_test.go`;
- `internal/geometry/pipeline.go`, `pipeline_test.go`;
- `internal/geometry/simplify.go`, `simplify_test.go`;
- `internal/geometry/soften.go`, `soften_test.go`;
- `internal/geometry/testdata/lod-spike/**`.

Everything else is read-only. In particular `.git`, `.mate`, `docs`,
`internal/catalog/**`, all T0A files, `cmd/lodbuild/**`,
`internal/geometry/serialize.go`, `serialize_test.go`, T1 ladder publication
files, T2 integration/mutation/approval fixtures and generated `node_modules`
are forbidden. Existing scratch is shared; preserve unrelated bytes and never
revert another owner's work. Integration target is `main`, but return without
staging, committing or lifecycle mutation.

## Context and doctrine pack

CTX-RUN-016 supplies the current P1 specification and Go doctrine. PLAN-010,
DEC-007, DEC-008 and this brief are the exact HOW.

T0A is immutable input, not an invitation to retune. Keep contribution
thresholds `110/900`, global oracle floors `.40/.42`, scale boundaries
`240/700`, `q=.01`, path caps `2200/7500`, complete-file maxima `2500/8000`,
candidate order, identity-set policy, projection, serializer and all five
representative artifact hashes exact. The default group-anchor set is empty.

The two inherited red tests are starting evidence:
`TestLODSpikeFullCorpus` reports AE/un/card `5910 > 2500`, and
`TestLODAQRUProjectionAlignedCheckpoint` expects AQ/card compact selection
where the incomplete production path falls back to source. Repair the generic
foundation; do not edit or weaken these assertions and do not branch on AE,
AQ, entity ID, profile or preset.

## Tasks

1. Verify the exact T0A gate/result/decision and the five representative
   artifact hashes before edits. Record hashes for every frozen T0A and
   serializer file and require them unchanged at handoff.
2. Reproduce both inherited red tests. Trace them through effective fitted
   scale, generic candidate lookup, q-aware source fallback and complete-file
   budget accounting. Fix the highest owning T0B seam only.
3. Complete the adapter, natural/contain fit, normalization, path, pipeline,
   simplification, softening and marker foundation needed by the T0B contract.
   Equal effective scale + resolved quality + byte policy must select the same
   candidate regardless of entity, profile, preset name or frame shape.
4. Add missing unit and mutation teeth inside T0B-owned tests for nil/source,
   unknown entity/profile/preset, degenerate geometry, portrait/landscape/
   square contain fit, tiny and huge sizes, q-aware source overflow, topology,
   winding, determinism and complete-file budget accounting.
5. Build a deterministic temporary full-catalog ladder from the frozen recipe,
   oracle and protected-visibility policy. It is T0B evidence only and must not
   publish T1 `v2.ladder.json`, manifest or generated Go initializer.
6. Generate exactly 996 ordinary outputs: every one of 249 entities, both
   `un`/`disputed` profiles and both `card`/`hero` preset inputs. Reject missing
   or duplicate keys. For every row record selected generic candidate,
   effective scale, natural bounds/ratio, uniform contain transform, topology,
   component provenance, omissions, IoU/recall, points, path bytes,
   deterministic complete-file bytes and output digest.
7. Require all compact rows `path<=2200` and complete file `<2500`; all
   standard rows `path<=7500` and complete file `<8000`. Emit stable
   distributions and exact maxima. No row may rely on source fallback for an
   ordinary automatic output or hide a typed failure.
8. Exercise arbitrary contain frames including `19x19`, tiny non-square,
   portrait, landscape, square and very large values. Prove uniform scale,
   centered containment, no distortion and fitted-scale-only candidate
   selection. Natural layout must retain each map's own aspect ratio.
9. Rebuild the 996-output evidence twice from identical inputs and require
   byte-identical outputs, manifest, metrics, maxima and sheet. Measure normal
   warm production on YMBPM3; require `<=120s` and record machine/runtime
   identity. Ordinary Go generation must work with network disabled and
   without Node, Mapshaper, CGO or external files/processes.
10. Render a legible, paginated or tiled full-catalog owner sheet from the
    exact accepted SVGs, with compact/standard examples and machine-linked
    manifest/receipt. Surface any visually identity-defining omission; do not
    add a group anchor without separate owner-reviewed data and dependent
    reruns.
11. Return exact changed paths, commands, test output, timings, maxima,
    artifact hashes and sheet path. Stop for owner approval; do not stage or
    create the T0 commit.

## Validation and evidence

Required checks:

- `GOCACHE=/private/tmp/country-map-go-cache GOWORK=off go test
  ./internal/geometry/cmd/lodbuild -run
  'Test(LODSpikeFullCorpus|LODAQRUProjectionAlignedCheckpoint)' -count=1`;
- all T0B-owned geometry package tests plus the frozen T0A focused gate;
- two independent 996-output builds and byte comparison of every output,
  manifest, metrics, sheet and machine receipt;
- exact row cardinality/key uniqueness, topology, provenance, oracle and budget
  validators;
- natural and arbitrary contain-frame matrix around both scale boundaries;
- warm 996-output production timing on YMBPM3;
- compile-only and offline ordinary-generation checks;
- frozen T0A, serializer, dependency and no-publication hash inventory.

The selected PLAN-010 gate is
`geometry-ladder-integrated-budget-visual-performance` at project tier
(`VAL-3`). Machine evidence must bind exact inputs, output identities,
distributions/maxima, timings, commands and exit codes. The full-catalog owner
approval is separate and cannot waive any machine failure.

## Constraints and forbidden actions

No threshold/oracle/recipe/protection/candidate-order/projection/q/serializer
change; no entity/profile/preset/frame branch; no vertex reinsertion, hand
editing, MakeValid, union/buffer, post-hoc SVG optimizer, lower precision,
higher budget, runtime repair or hidden fallback. No Node/Mapshaper/network/
external-process dependency in ordinary Go generation.

`card` and `hero` remain data presets, never hardcoded output dimensions or
runtime modes. The CLI/library caller may later request any positive size;
T0B proves the foundation is distortion-free but does not implement P3/P4/P5
delivery surfaces.

Do not add or populate a group-anchor override, publish T1 artifacts, touch T2
tests, stage `node_modules`, run blanket Git staging or commit before both the
machine gate and owner full-catalog approval.

## Stop escalation and resume conditions

Stop and return blocked evidence if a repair requires changing any frozen T0A
byte, serializer, budget, q, projection, candidate order or owner-approved
threshold; no generic candidate can satisfy one or more ordinary rows; a
dominant/anchor/visible protected component cannot fit; output count or keys
drift; determinism fails; runtime exceeds `120s` after one bounded
foundation-level optimization; or ordinary generation needs an external
runtime dependency.

Do not hide a full-catalog failure with an entity exception or relabel it as a
visual-only concern. On machine PASS, return the exact review sheet and wait
for owner approval. Owner rejection or a proposed non-empty group anchor
returns to a bounded successor run and reruns both T0A-dependent and T0B
evidence. Owner approval permits the coordinator to create the one atomic T0
commit from the exact T0A/T0B path union.

<!-- MATE:extensions — generated by composition from selected profiles and concerns -->

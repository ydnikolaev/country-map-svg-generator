---
# SCAFFOLDED by mate from a versioned governed template; AUTHORABLE INSTANCE.
schema_version: "1.2.0"
template_version: "1.2.0"
kind: "run-brief"
id: "RUN-006"
epic: "country-map-svg-generator"
spec: "P2"
run: "RUN-006"
status: draft
profiles: []
concerns: []
inputs: ["PLAN-006", "CTX-RUN-008"]
---
# RUN-006 — Projected LOD representation brake

## Authority and accepted inputs

- Accepted PLAN-006 revision:
  `5df964761f2591b49776ca7e93209e2c1dbb45a22e3e6771f0fad2cd7db196d3`.
- Accepted PLAN-006 manifest:
  `b62f645fb2ae84a3a8ff07a670299eee1764bedd0c8d5cc5d1264331881ff190`.
- Runtime context CTX-RUN-008:
  `4a9124a2c8cd3d61be7f6ced6b9aa9d62cd78c6cc6aee503079b28f7ca622c84`.
- Product baseline from PLAN-006 is commit
  `7ab35331e3e30291769fd178d7b2f0a9aa91c3df`, tree
  `50b5d30f4ee904d6c53c92a8610a988f2526c5d3`; lifecycle head at run
  authoring is `b04e7da`.
- DEC-003, DEC-004, refining DEC-005, AM-001, AM-002, P1/CTR-001,
  exact P1 RUN-001/RUN-002/checkpoint/hashes and interrupted RUN-004/RUN-005
  bind through PLAN-006. Readiness binds atomically at run start.

## Objective and completion boundary

Deliver P2 CTR-002 with natural-aspect `tight`, arbitrary distortion-free
`contain`, compact/standard/source LOD, full-source
retention/restoration/markers, final-representation topology and deviation,
deterministic fixed-grid commands and generic byte budgets.

Only T0 is initially dispatchable. It replaces geographic LOD candidates with
per-geometry centered, unrotated LAEA planar candidates and implements the
DEC-005 q-aware source finalizer. It first isolates representation from bytes on
AQ/RU with serializer code frozen. Only if unlimited and budgeted final paths
pass does it run the all-corpus matrix and prepare the owner visual sheet. T1
publication and T2 closure evidence remain blocked.

P3 presentation and CLI parsing, P4 browser integration, public packaging and
deferred `detail` are excluded.

## Scope ownership and footprint

T0 writable paths are exactly PLAN-006 T0:

- `go.mod`, `go.sum`;
- `internal/geometry/cmd/lodbuild/**`, `internal/geometry/lod/tool/**`,
  `internal/geometry/lod/v1.recipe.json`,
  `internal/geometry/testdata/lod-spike/**`;
- the specifically leased `internal/geometry` runtime, test, policy and override
  files/directories enumerated by the accepted manifest.

`internal/geometry/serialize.go` and `serialize_test.go` are read-only controls.
T1 artifact publication files and T2 evidence/Makefile files are forbidden until
their predecessor receipt. `data/**`, `internal/catalog/**`, `cmd/**`,
`.git/**`, `.mate/**`, lifecycle docs and another task's lease are forbidden to
implementers. Integration target is `main`; implementers return scoped changes
without commits or lifecycle mutation.

## Context and doctrine pack

CTX-RUN-008 supplies the current run doctrine; PLAN-006 supplies the exact HOW.
Mapshaper 0.7.44, Node and npm are maintainer-only. Ordinary Go generation and
`make check` remain offline and never execute them.

All existing `go.mod`, `go.sum` and `internal/geometry/**` bytes are uncommitted
scratch until reconciled inside T0. Reuse requires fresh focused proof. Chat
history is not authority.

## Tasks

1. Reconcile the existing scratch without broad rewrite.
2. Change the maintainer builder/artifact seam to centered unrotated LAEA
   coordinates with explicit Mapshaper planar simplification, exact metadata and
   runtime binding; preserve `64/256` and thresholds `240/700`.
3. Route compact, standard and source through the final q/topology/deviation
   guard; selection-only must execute the same non-serialization checks.
4. Implement DEC-005 source finalization with q displacement reserved,
   monotonic finer search and explicit `source_reduced` provenance.
5. Run the frozen-serializer AQ/RU experiment first: equivalent unlimited input,
   then normal card/hero budgets.
6. Continue only after that checkpoint to the complete matrix, two rebuilds and
   representative visual sheet. Stop for owner approval before any T0 product
   commit or T1.

## Validation and evidence

The first checkpoint records AQ/un card and hero through compact/standard plus
forced source for AQ card/hero and RU card. For every unlimited and budgeted case
record requested/selected tier, coordinate-space identity, raw/final deviation,
resolved/source tolerance, q, restorations, fallbacks, points, path bytes,
strict parser round-trip and runtime.

- Unlimited topology/protection/deviation failure means representation failed:
  stop without serializer changes.
- Unlimited pass plus budget failure means representation passed and bytes are a
  new successor input: stop without serializer changes.
- Only complete unlimited and `2500/8000` success unlocks the broad matrix.

The broad brake covers 249 entities, both profiles, card/hero, long sides
`90,128,160,240,500,640,700,1024,2048`, softening on/off, and arbitrary
`19x19`/`10000x12345` contain frames. Evidence includes final path maxima, tiers,
fallbacks, source reductions, restorations, deviations, runtime, artifact hashes
and two byte-identical rebuilds. The visual sheet covers
RU/CL/AR/AU/AQ/CA/ID plus island states. Machine success never substitutes for
owner visual approval.

## Constraints and forbidden actions

- Full P1 determines projector, aspect, fit, retention, protected anchors,
  marker transform and deviation authority.
- Builder candidates use exact centered LAEA metadata and explicit planar mode;
  rotation applies consistently under the declared y-axis convention.
- Selection uses effective fitted scale and availability only; never ISO,
  profile, preset or frame shape.
- Keep q exactly `0.01`, ratio at or below `0.012`, LOD `64/256`, thresholds
  `240/700`, hard budgets `2500/8000` and advisory `2200/7500`.
- Restore required parts before validation. Proper crossings, changed winding,
  escaped holes, collapsed rings, lost protection or excessive final symmetric
  deviation fail.
- Source reduction is allowed only by DEC-005: exact retained P1 input,
  q displacement reservation, monotonic finer search, complete final guard and
  explicit provenance.
- Never use MakeValid, union/buffer repair, ad-hoc vertex movement,
  country branches, manual geography, a new tier/dependency, changed serializer,
  smaller q, larger tolerance, silent budget overflow, SVGO or runtime external
  processes.

## Stop escalation and resume conditions

Stop before mutation for a new artifact coordinate model beyond PLAN-006, new
tier/dependency/tool setting, resolution/threshold/q/budget/formula change,
selection input, serializer grammar or runtime external process. Changes to
natural/contain semantics, P1 authority, protected policy, presentation-free
CTR-002 or offline single-binary contract require an amendment/decision.

T0 stops without product commit on either explicit small-brake failure, any
broad topology/deviation/protection/provenance/budget/rebuild failure, or owner
visual rejection. After repeated speculative fixes, isolate the smallest fixture
and read the owning canonical documentation. Resume only from PLAN-006,
DEC-005, this brief, CTX-RUN-008, exact lease, scoped diff and last verified
result.

<!-- MATE:extensions — generated by composition from selected profiles and concerns -->

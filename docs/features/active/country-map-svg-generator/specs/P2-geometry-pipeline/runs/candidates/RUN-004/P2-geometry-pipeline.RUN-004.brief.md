---
# SCAFFOLDED by mate from a versioned governed template; AUTHORABLE INSTANCE.
schema_version: "1.2.0"
template_version: "1.2.0"
kind: "run-brief"
id: "RUN-004"
epic: "country-map-svg-generator"
spec: "P2"
run: "RUN-004"
status: draft
profiles: []
concerns: []
inputs: ["PLAN-004", "CTX-RUN-006"]
---
# RUN-004 — Scale-relative LOD geometry delivery

## Authority and accepted inputs

- Accepted PLAN-004 revision:
  `48791fbd35f22b030817852734410e108f86abcfb52c6bd456a9d645633efb6d`.
- Accepted PLAN-004 manifest:
  `fefffaf64ce43ec6ba2bf5a79ad4ffa7ec63e9867c61cd5d8332670c9b339893`.
- Runtime context CTX-RUN-006:
  `719327d56dcd86109ed0293c77f2fca96c8331eb6524d87eeb56d1b28dee28ce`.
- Product baseline commit `bd7af4f40c230ecbc673ad99745857283b4d2c63`,
  tree `983eb7034ec1e04348dff97fbc4e35ab794d497b`; lifecycle head at run
  authoring is `d0a01e96fc37e5cd6994755c8862db7e83a33e6c`.
- DEC-003, DEC-004, AM-001, AM-002, P1/CTR-001 and interrupted RUN-003
  are binding through PLAN-004. Readiness binds atomically at run start.

## Objective and completion boundary

Deliver CTR-002 with natural-aspect `tight`, arbitrary distortion-free `contain`,
one versioned fitted-scale quality policy, compact/standard/source LOD,
full-source retention/restoration, robust topology/deviation guards,
deterministic minified fixed-grid commands, generic hard budgets, bounded
softening and common-transform markers.

T0 first reconciles the RUN-003 scratch and calibrates only the global
scale-relative formula `max(floor_px, ratio * effective_long_side)` inside
PLAN-004 ranges. It injects all-283 candidates into the exact production path,
covers all 249 entities, both profiles and every required size/frame, requires
card `<=2500`, hero `<=8000`, and stops for owner approval of the representative
sheet. T1 cannot publish before T0 passes. P3 styling/CLI parsing, P4 browser
integration, public packaging and deferred `detail` are excluded.

## Scope ownership and footprint

Writable product paths are the exact PLAN-004 task union: `Makefile`, `go.mod`,
`go.sum` and its declared `internal/geometry/**` files. Sequential leases are:

- T0: quality policy, builder/tool/recipe, injected LOD production path,
  projection/layout, retention/restoration, topology/deviation, serializer,
  budgets, softening, presets, overrides, markers and spike evidence;
- T1: compact/standard/manifest artifacts, embedding adapter and rebuild check;
- T2: approval/integration/oracle/mutation evidence and `Makefile`.

`data/**` and `internal/catalog/**` are read-only. `cmd/**`, `.git/**`, `.mate/**`,
lifecycle docs and another task's lease are forbidden to implementers.
Integration target is `main`; implementers return scoped changes without
lifecycle mutation.

## Context and doctrine pack

CTX-RUN-006 supplies current implementation context; PLAN-004 supplies the exact
successor HOW. Mapshaper 0.7.44, Node and npm are maintainer-only. Ordinary Go
generation and `make check` stay offline and never execute them.

RUN-003 Go files are untrusted scratch until reconciled inside the active lease.
Its exact topology/cache/serializer work may be reused only with fresh tests.
Chat history is not authority.

## Tasks

1. T0: move automatic defaults to versioned data, implement and calibrate the
   scale-relative policy, pass the all-corpus integrated brake and obtain owner
   visual approval.
2. T1: publish/embed the exact T0-frozen all-283 artifacts and manifest.
3. T2: allocate full P2 evidence and pass offline `make check`.

Only T0 is initially dispatchable. Every later task requires the prior exact
receipt and scoped product commit.

## Validation and evidence

- T0 focused tests cover policy parsing/resolution, explicit-quality precedence,
  topology, serializer, LOD selection/cache/fallback and the all-corpus matrix.
- T0 evidence names all 283 IDs, every entity/profile, required scales and
  frames, final path maxima, tiers, fallbacks, restorations, deviations and
  resolved tolerances, runtime, hashes, two rebuilds and visual sheet.
- T1 proves two clean rebuilds equal committed bytes and validates all source,
  tool, recipe, lockfile, hash, coverage and omission bindings offline.
- T2 proves D3 parity, natural/arbitrary layouts, policy selection, restoration,
  mutations, visual approval, overrides, markers and the project ceiling.

Representative green cases never replace the all-corpus brake. Visual approval
is mandatory evidence and cannot be inferred from structural tests.

## Constraints and forbidden actions

- Automatic simplification uses only effective fitted scale and the versioned
  floor/ratio; never ISO, profile, preset name or frame shape.
- Keep `floor_px` in `[0.75,1.00]`, ratio in `[0.006,0.010]`, compact threshold
  in `[160,320]`, standard threshold in `[500,900]`, recipe resolutions `64/256`
  and canonical precision `0.01px`.
- Explicit non-auto quality retains its absolute `1.25px` safety bound.
- Full P1 geometry determines projector, aspect, fit, retention and markers.
- Restore required source components before validation; shared endpoints require
  the same source vertex; crossings, touches, winding changes, escaped holes and
  collapse fail.
- Symmetric deviation, softening displacement, fixed-grid topology and strict
  serializer round trip fail closed. No runtime RDP repair, SVGO or post-hoc
  optimization.
- `MaxPathBytes` stays generic data; fallback code cannot inspect preset names.
- Never country-branch, hand-redraw, bridge or synthesize geography.
- Never add runtime Node/Python/GDAL/PROJ/CGO/network or weaken a topology,
  protection, budget, deterministic rebuild, visual or project gate.

## Stop escalation and resume conditions

Stop before mutation for a new tier, dependency/tool setting, recipe resolution,
threshold range, tolerance formula family, quantization, hard budget, selection
input, serializer grammar or runtime external process. Stop for changes to
natural/contain semantics, P1 authority, protected policy, presentation-free
CTR-002 or the offline single-binary contract.

T0 stops without product commit if any hard maximum, topology, deviation,
protection, provenance, visual, runtime or rebuild brake fails. After repeated
speculative fixes, isolate the smallest fixture and read the owning canonical
documentation. Resume only from PLAN-004, this brief, CTX-RUN-006, the exact
lease, current scoped diff and last verified result.

<!-- MATE:extensions — generated by composition from selected profiles and concerns -->

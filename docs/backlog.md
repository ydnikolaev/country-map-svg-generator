# Backlog

<!-- Seeded once by mate (structure standard: .mate/doctrine/code/structure.md).
     This file is YOURS: mate will never touch it again. Role: the open-items
     queue — one bullet per item, `- [ ]` open / `- [x]` resolved; archive
     resolved rows to a sibling file when this grows. -->

## Open

- [ ] **Detail SVG profile** — после запуска `hero` и `card` добавить отдельный
  профиль для декоративных карт размером до 1600 CSS px. Перед реализацией
  пересмотреть бюджеты веса и детализации по фактическим метрикам полного
  каталога; не расширять MVP и не ослаблять его performance-gates.
